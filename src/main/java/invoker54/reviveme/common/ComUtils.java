package invoker54.reviveme.common;

public class ComUtils {

//    private static Map<UUID, FallenCapability> serverCaps = new HashMap<>();
//    private static Map<UUID, FallenCapability> clientCaps = new HashMap<>();
//
//    public static void SetCap(PlayerEntity player){
//        //System.out.println(player.getClass());
//
//        if (player instanceof ClientPlayerEntity)
//            clientCaps.put(player.getUUID(), player.getCapability(FallenProvider.FALLENDATA).orElseThrow(NullPointerException::new));
//
//        else serverCaps.put(player.getUUID(),
//                player.getCapability(FallenProvider.FALLENDATA).orElseThrow(NullPointerException::new));
//    }
//
//    public static FallenCapability GetFallCap(PlayerEntity playerEntity) {
//        if (playerEntity instanceof ClientPlayerEntity) {
//            if (clientCaps.get(playerEntity.getUUID()) == null)
//                SetCap(playerEntity);
//
//            return clientCaps.get(playerEntity.getUUID());
//        } else {
//            if(serverCaps.get(playerEntity.getUUID()) == null)
//                SetCap(playerEntity);
//
//            return serverCaps.get(playerEntity);
//        }
//    }





}
